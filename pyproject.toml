[project]
name = "smart-delivery-ai"
version = "0.1.0"
description = "Smart Delivery API"

dependencies = [
    "fastapi",
    "uvicorn",
    "pydantic",
    "requests"
]

[project.scripts]
server = "server.app:main"