name: delivery_optimizer
description: Smart Delivery Route Optimization Environment

endpoints:
  reset: /reset
  step: /step
  state: /state

tasks:
  - easy
  - medium
  - hard

openapi: 3.0.0
info:
  title: Smart Delivery AI
  version: 1.0.0

paths:
  /openenv/reset:
    post:
      summary: Reset environment